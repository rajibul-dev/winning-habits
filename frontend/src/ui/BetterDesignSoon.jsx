import Info from "./Info";

export default function BetterDesignSoon() {
  return (
    <Info>
      Better design will come if this app becomes a successful project (if users
      actually uses it and find it somewhat useful)!
    </Info>
  );
}
